upstream django {
    server web:8000;
}

# HTTP сервер для продакшена - для редиректа на HTTPS и проверки Let's Encrypt
server {
    listen 80;
    server_name 2a0aa2c90f37.vps.myjino.ru;
    
    # Для Let's Encrypt проверки
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }
    
    # Редирект на HTTPS для всего остального
    location / {
        return 301 https://$host$request_uri;
    }
}

# HTTPS сервер для продакшена
server {
    listen 443 ssl;
    server_name 2a0aa2c90f37.vps.myjino.ru;
    
    # SSL сертификаты
    ssl_certificate /etc/nginx/ssl/live/2a0aa2c90f37.vps.myjino.ru/fullchain.pem;
    ssl_certificate_key /etc/nginx/ssl/live/2a0aa2c90f37.vps.myjino.ru/privkey.pem;
    
    # Оптимизация SSL
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384;
    ssl_session_timeout 1d;
    ssl_session_cache shared:SSL:10m;
    ssl_session_tickets off;
    
    client_max_body_size 100M;

    location /static/ {
        alias /app/staticfiles/;
    }

    location /media/ {
        alias /app/media/;
    }

    location / {
        proxy_pass http://django;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
} 